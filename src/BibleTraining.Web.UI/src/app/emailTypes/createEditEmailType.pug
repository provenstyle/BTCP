div
    .row.wrapper.border-bottom.white-bg.page-heading
        .col-lg-10
            h2 {{ctrl.title}}
            ol.breadcrumb
                li
                    a(href="#!/emailTypes/showEmailTypes") EmailTypes
                li(ng-if="ctrl.isEdit")
                    a(href="#!/createEmailType/showCreateEmailType") Create EmailType
                li.active(ng-if="ctrl.isCreate")
                    strong Create EmailType

    .wrapper.wrapper-content
        .row
            .col-lg-12
                .ibox-content
                    form.form-horizontal
                        .form-group
                            label.col-lg-2.control-label Name
                            .col-lg-10
                                input.form-control(type="text" placeholder="Name" ng-model="ctrl.emailType.name")
                                div.text-danger(ng-messages='ctrl.$validation.emailType.name.errors')
                                    div.small(ng-message='presence') EmailType name is required
                        .form-group
                            label.col-lg-2.control-label Description
                            .col-lg-10
                                input.form-control(type="text" placeholder="Description" ng-model="ctrl.emailType.description")
                                div.text-danger(ng-messages='ctrl.$validation.emailType.description.errors')
                                    div.small(ng-message='presence') EmailType description is required
                        .form-group
                            .col-lg-offset-2.col-lg-10
                                 button.btn.btn-sm.btn-white(type="submit" ng-click="ctrl.cancel()") Cancel
                                 button.btn.btn-sm.btn-primary(type="submit" ng-click="ctrl.save()") {{ctrl.buttonText}}
                                 a.btn.btn-link.pull-right(ng-if="ctrl.idEdit" ng-click="ctrl.remove()") Delete
