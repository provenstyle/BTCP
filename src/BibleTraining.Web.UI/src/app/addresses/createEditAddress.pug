div
    .row.wrapper.border-bottom.white-bg.page-heading
        .col-lg-10
            h2 {{ctrl.title}}
            ol.breadcrumb
                li
                    a(href="#!/addresses/showAddresses") Addresses
                li(ng-if="ctrl.isEdit")
                    a(href="#!/createAddress/showCreateAddress") Create Address
                li.active(ng-if="ctrl.isCreate")
                    strong Create Address

    .wrapper.wrapper-content
        .row
            .col-lg-12
                .ibox-content
                    form.form-horizontal
                        .form-group
                            label.col-lg-2.control-label Name
                            .col-lg-10
                                input.form-control(type="text" placeholder="Name" ng-model="ctrl.address.name")
                                div.text-danger(ng-messages='ctrl.$validation.address.name.errors')
                                    div.small(ng-message='presence') Address name is required
                        .form-group
                            label.col-lg-2.control-label Description
                            .col-lg-10
                                input.form-control(type="text" placeholder="Description" ng-model="ctrl.address.description")
                                div.text-danger(ng-messages='ctrl.$validation.address.description.errors')
                                    div.small(ng-message='presence') Address description is required
                        .form-group
                            .col-lg-offset-2.col-lg-10
                                 button.btn.btn-sm.btn-white(type="submit" ng-click="ctrl.cancel()") Cancel
                                 button.btn.btn-sm.btn-primary(type="submit" ng-click="ctrl.save()") {{ctrl.buttonText}}
                                 a.btn.btn-link.pull-right(ng-if="ctrl.idEdit" ng-click="ctrl.remove()") Delete
