div
    .row.wrapper.border-bottom.white-bg.page-heading
        .col-lg-10
            h2 Address
            ol.breadcrumb
                li
                    a(href="#!/addresses/showAddresses") Addresses
                li
                    a(href="#!/createAddress/showCreateAddress") Create Address

    .wrapper.wrapper-content
        .row
            .col-lg-12
                .ibox-content
                    button.btn.btn-primary.btn-xs.pull-right(ng-click="ctrl.goToEdit()") Edit Address
                    h3 {{ ctrl.address.name }}
                    p  {{ ctrl.address.description }}
